export type CartRowRemoveProductComplexible = {
  productId: string;
  // quantity: number;
};

export type CartRowRemoveProductTxData = {
  userId: string;
  productId: string;
  // quantity: number;
};

export type StoreRemoveTxPayload = {
  selector: StoreRemoveSelector;
};

export type StoreRemoveTxDTO = {
  selector: StoreRemoveSelector;
};

// NOTE: Selector
export type StoreRemoveSelector = {
  id: string;
};

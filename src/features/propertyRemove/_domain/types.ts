export type PropertyRemoveTxPayload = {
  selector: PropertyRemoveSelector;
};

export type PropertyRemoveTxDTO = {
  selector: PropertyRemoveSelector;
};

// NOTE: Selector
export type PropertyRemoveSelector = {
  id: string;
};

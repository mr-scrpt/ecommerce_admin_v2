export type ConsumerRemoveTxPayload = {
  selector: ConsumerRemoveSelector;
};

export type ConsumerRemoveTxDTO = {
  selector: ConsumerRemoveSelector;
};

// NOTE: Selector
export type ConsumerRemoveSelector = {
  id: string;
};

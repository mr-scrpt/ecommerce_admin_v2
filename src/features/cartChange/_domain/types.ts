export type CartRowChangeCountProductComplexible = {
  productId: string;
  quantity: number;
};

export type CartRowChangeCountProductTxData = {
  userId: string;
  productId: string;
  quantity: number;
};

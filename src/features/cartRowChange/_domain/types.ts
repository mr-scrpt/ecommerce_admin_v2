export type CartRowChangeQuantityComplexible = {
  productId: string;
  quantity: number;
};

export type CartRowChangeQuantityTxData = {
  userId: string;
  productId: string;
  quantity: number;
};
